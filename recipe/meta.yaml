{% set name = "brainglobe-utils" %}
{% set version = "0.3.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/brainglobe-utils-{{ version }}.tar.gz
  sha256: 42d6097c35a2ff6c1043290919a4dbc9bd32fe9ac717312307416ad3e4a4969d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 1

requirements:
  host:
    - python >=3.8.0
    - setuptools >=45
    - wheel
    - setuptools-scm >=6.2
    - pip
  run:
    - python >=3.8.0
    - natsort
    - pandas
    - psutil
    - slurmio
    - configobj
    - tqdm
    - pyyaml
    - scipy
    - scikit-image
    - tifffile
    - imio
    - numpy
    - qtpy
    - superqt
    - bg-atlasapi
    - bg-space

test:
  imports:
    - brainglobe_utils
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  summary: Shared general purpose tools for the BrainGlobe project
  home: https://github.com/brainglobe/BrainGlobe-utils
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - willGraham01
    - adamltyson
